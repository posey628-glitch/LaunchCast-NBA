# Python
__pycache__/
*.py[cod]
*$py.class
.Python
build/
dist/
*.egg-info/

# Streamlit secrets — NEVER commit these (owner key, any tokens)
.streamlit/secrets.toml

# Environment
.env
.env.*
venv/
.venv/

# OS
.DS_Store
Thumbs.db

# Data caches / scratch
work/
*.csv
*.xlsx
cache/
*.pkl

# NOTE: .github/ is intentionally NOT ignored — workflows must commit.
# (This was the MLB bug: .gitignore excluded .github/ so automation never ran.)
